---
description: Configure LemGen API token.
---

Guide the user to create an API key at https://lemgen.org, then set `LEMGEN_API_TOKEN` in their MCP host config.
For Claude Code plugin installs, tell them to restart Claude Code after changing MCP env vars.
